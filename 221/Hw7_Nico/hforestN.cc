
//#include "hforest.hh"

	//int size() { //return how many trees are in the forest.
	
	}

	//void add_tree(tree_ptr_t) { //Take a pointer to an HTree (of type HTree::tree_ptr_t) and add it to the forest.
	
	}

	//HTree::tree_ptr_t pop_tree() {//return a pointer to the HTree with the lowest value in the root node, and remove it from the forest.
		
	}

	//bool HForest::tcompare_trees(tree_ptr_t t1, tree_ptr_t t2) {
    	//return t1->get_value() < t2->get_value();
	}